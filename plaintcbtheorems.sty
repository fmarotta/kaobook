\ProvidesPackage{plaintcbtheorems}

% Plain ams* theorems wrapped inside tcolorboxes

% Use in combination with plaintheorems! Load first plaintheorems, then
% plaintcbtheorems.

\usepackage[most]{tcolorbox}

% Wrap amsthm's theorem environment in a tcolorbox
\tcolorboxenvironment{theorem}{
  breakable,
  enhanced jigsaw,
  sharp corners,
  %attach boxed title to top left={
    %yshifttext=-1mm
  %},
  %fonttitle=\bfseries,
  %fontupper=\textsc,
  colback=orange,
  colframe=orange!50!black,
  %before skip=10pt,
  %after skip=10pt,
  %interior hidden,
  %colframe=orange,
  frame hidden,
  %coltitle=black,
  %colbacktitle=orange,
  %titlerule=.5pt,
  %titlerule style=black,
  %boxed title style={
    %sharp corners,
    %size=small,
    %colback=orange!75!black,
    %colframe=orange!75!black,
  %} 
}

% Wrap amsthm's definition environment inside a tcolorbox
\tcolorboxenvironment{definition}{% 'definition' from 'amsthm'
	blanker,breakable,left=4mm,
	before skip=10pt,after skip=23pt,
	borderline west={1mm}{0pt}{LightGray}
}

\tcolorboxenvironment{proposition}{
  breakable,
  enhanced jigsaw,
  sharp corners,
  colback=orange,
  colframe=orange!50!black,
  frame hidden,
}
